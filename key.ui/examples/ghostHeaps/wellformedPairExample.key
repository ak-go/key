\javaSource "./src";

\programVariables{
    bank.Transaction t;
}

\include "./ghostHeapRules.key";

\problem{
    t != null
    & t.<created> = TRUE
    & t.amount = 100
    & t.active@gHeap = TRUE
    & wellFormedPair(heap, gHeap)

    -> \<{ t.active = false;
           t.amount = 0;}\>
       ( t.amount = 0
        & t.active@gHeap = FALSE
        & wellFormedPair(heap, gHeap) )
}